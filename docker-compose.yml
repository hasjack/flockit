# FlockIt docs site.
# Today: static nginx (Vite build).
# Midterm: add an `api` service for login and uncomment the /api proxy in
# client/nginx.conf — keep both on the same compose network.
services:
  web:
    build:
      context: ./client
      dockerfile: Dockerfile
    image: flockit-web:latest
    container_name: flockit-web
    ports:
      - "${FLOCKIT_PORT:-8080}:80"
    restart: unless-stopped
    networks:
      - flockit

  # --- midterm: login / account API ---
  # api:
  #   build:
  #     context: ./api
  #     dockerfile: Dockerfile
  #   image: flockit-api:latest
  #   container_name: flockit-api
  #   environment:
  #     PORT: 3000
  #     # DATABASE_URL: ...
  #     # JWT_SECRET: ...
  #     # FRONTEND_ORIGIN: https://flockit.example.com
  #   expose:
  #     - "3000"
  #   restart: unless-stopped
  #   networks:
  #     - flockit
  #   depends_on:
  #     - web

networks:
  flockit:
    name: flockit-network
